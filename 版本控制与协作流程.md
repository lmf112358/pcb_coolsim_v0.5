# PCB-CoolSim 版本控制与协作流程

> 版本：1.0
> 日期：2026-07-10
> 状态：已完成

---

## 一、Git 仓库配置

### 1.1 仓库初始化

```bash
# 初始化本地仓库
git init

# 添加所有文件
git add .

# 创建初始提交
git commit -m "feat: 初始化 PCB-CoolSim v0.5 项目结构"
```

### 1.2 远程仓库关联

```bash
# 添加远程仓库（GitHub）
git remote add origin https://github.com/your-org/pcb_coolsim_v0.5.git

# 推送初始提交
git push -u origin main
```

### 1.3 .gitignore 配置

已创建 `.gitignore` 文件，排除：
- Python 字节码和缓存文件
- Node.js 依赖和构建输出
- 虚拟环境和环境变量
- IDE 配置文件
- 日志和临时文件
- 数据库文件
- 大型测试数据文件（可选）

---

## 二、分支策略

### 2.1 分支类型

| 分支类型 | 命名规范 | 用途 | 生命周期 |
|----------|----------|------|----------|
| 主分支 | `main` | 生产环境代码，始终保持稳定 | 永久 |
| 开发分支 | `develop` | 开发环境代码，集成最新功能 | 永久 |
| 功能分支 | `feature/*` | 开发新功能 | 短期 |
| 修复分支 | `bugfix/*` | 修复 bug | 短期 |
| 发布分支 | `release/*` | 准备发布版本 | 短期 |
| 热修复分支 | `hotfix/*` | 紧急修复生产问题 | 短期 |

### 2.2 分支命名规范

```bash
# 功能分支
feature/F1-001-user-authentication
feature/F2-001-data-collection
feature/F4-001-static-calculation

# 修复分支
bugfix/F1-001-login-error
bugfix/F4-001-calculation-precision

# 发布分支
release/v0.5.1
release/v0.5.2

# 热修复分支
hotfix/v0.5.1-security-fix
hotfix/v0.5.1-critical-bug
```

### 2.3 分支工作流程

```
main (生产)
  ↑
  │ merge (release)
  │
develop (开发)
  ↑
  │ merge (feature/bugfix)
  │
feature/* / bugfix/*
```

---

## 三、提交规范

### 3.1 提交信息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 3.2 类型说明

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat(auth): 添加用户登录功能` |
| `fix` | 修复 bug | `fix(calculation): 修复冷量计算精度问题` |
| `docs` | 文档更新 | `docs(API): 更新接口文档` |
| `style` | 代码格式（不影响功能） | `style(backend): 格式化代码` |
| `refactor` | 重构（既不是新功能也不是修复） | `refactor(calculation): 重构计算引擎` |
| `perf` | 性能优化 | `perf(database): 优化查询性能` |
| `test` | 测试相关 | `test(calculation): 添加单元测试` |
| `chore` | 构建过程或辅助工具的变动 | `chore(deps): 更新依赖版本` |
| `ci` | CI 配置变更 | `ci(github): 配置 GitHub Actions` |
| `revert` | 回滚 | `revert: 回滚上一个提交` |

### 3.3 范围说明

| 范围 | 说明 |
|------|------|
| `auth` | 认证授权 |
| `project` | 项目管理 |
| `room` | 房间管理 |
| `calculation` | 计算引擎 |
| `simulation` | 动态仿真 |
| `export` | 报告导出 |
| `frontend` | 前端 |
| `backend` | 后端 |
| `database` | 数据库 |
| `docs` | 文档 |
| `ci` | CI/CD |
| `deps` | 依赖 |

### 3.4 提交示例

```bash
# 新功能
git commit -m "feat(auth): 添加 JWT 认证功能

- 实现用户登录接口
- 添加 Token 刷新机制
- 实现权限验证中间件

PRD 引用：F1-001
Spec 引用：模块-01-账户与项目管理.md Section 2"

# 修复 bug
git commit -m "fix(calculation): 修复静态冷量计算精度问题

- 修正焓值计算公式
- 优化浮点数精度处理
- 添加精度验证测试

PRD 引用：F4-001
测试用例：TC-CALC-001"

# 文档更新
git commit -m "docs(API): 更新认证接口文档

- 添加请求/响应格式说明
- 补充错误码定义
- 更新使用示例

PRD 引用：F1-001"
```

---

## 四、Pull Request 规则

### 4.1 PR 标题格式

```
<type>(<scope>): <简短描述>

示例：
feat(auth): 添加用户认证功能
fix(calculation): 修复冷量计算精度问题
docs(API): 更新接口文档
```

### 4.2 PR 描述模板

```markdown
## 变更说明

### 变更类型
- [ ] 新功能 (feat)
- [ ] Bug 修复 (fix)
- [ ] 文档更新 (docs)
- [ ] 代码重构 (refactor)
- [ ] 性能优化 (perf)
- [ ] 测试相关 (test)
- [ ] 其他 (chore)

### 变更内容
- [变更 1]
- [变更 2]
- [变更 3]

### 关联需求
- PRD 引用：[F1-001, F2-001, ...]
- Spec 引用：[模块-01-账户与项目管理.md, ...]
- 测试用例：[TC-AUTH-001, TC-AUTH-002, ...]

## 测试情况

### 单元测试
- [ ] 新增测试用例
- [ ] 所有测试通过
- [ ] 覆盖率达标（≥80%）

### 集成测试
- [ ] 接口测试通过
- [ ] 数据库测试通过
- [ ] 端到端测试通过

### 手动测试
- [ ] 功能验证通过
- [ ] 性能验证通过
- [ ] 安全验证通过

## 代码审查清单

### 代码质量
- [ ] 代码符合规范
- [ ] 无重复代码
- [ ] 无魔法数字/字符串
- [ ] 适当的注释

### 安全性
- [ ] 无 SQL 注入风险
- [ ] 无 XSS 风险
- [ ] 敏感信息已加密
- [ ] 权限验证完整

### 性能
- [ ] 无 N+1 查询问题
- [ ] 适当的缓存策略
- [ ] 无内存泄漏风险

### 文档
- [ ] API 文档已更新
- [ ] 代码注释已添加
- [ ] README 已更新（如需要）

## 依赖变更

### 新增依赖
- [依赖 1]: [版本] - [用途]
- [依赖 2]: [版本] - [用途]

### 更新依赖
- [依赖 1]: [旧版本] → [新版本]

### 删除依赖
- [依赖 1]: [原因]

## 部署说明

### 环境要求
- [环境要求 1]
- [环境要求 2]

### 数据库变更
- [ ] 新增表
- [ ] 修改表结构
- [ ] 数据迁移脚本

### 配置变更
- [ ] 环境变量
- [ ] 配置文件
- [ ] 服务配置

## 回滚方案

### 回滚步骤
1. [步骤 1]
2. [步骤 2]
3. [步骤 3]

### 回滚影响
- [影响 1]
- [影响 2]

## 相关 PR

- PR #123: [PR 描述]
- PR #456: [PR 描述]

## 截图/录屏

[如适用，添加截图或录屏]

## 其他说明

[其他需要说明的内容]
```

### 4.3 PR 规则

#### 分支规则

1. **功能分支**：从 `develop` 分支创建
2. **修复分支**：从 `develop` 分支创建
3. **热修复分支**：从 `main` 分支创建
4. **发布分支**：从 `develop` 分支创建

#### 命名规则

```
feature/F1-001-user-authentication
bugfix/F4-001-calculation-precision
hotfix/v0.5.1-security-fix
release/v0.5.1
```

#### 提交规则

1. **一个 PR 一个功能/修复**：不要混合多个不相关的变更
2. **提交信息规范**：遵循提交信息格式
3. **关联需求**：必须关联 PRD 和 Spec
4. **测试覆盖**：必须包含测试用例
5. **文档更新**：必须更新相关文档

#### 审查规则

1. **至少两人审查**：至少需要两人 Approve
2. **所有检查通过**：CI 检查必须全部通过
3. **无冲突**：PR 必须无冲突
4. **代码质量**：必须符合代码规范
5. **测试覆盖**：测试覆盖率必须达标

#### 合并规则

1. **Squash and Merge**：功能分支使用 Squash and Merge
2. **Merge Commit**：发布分支使用 Merge Commit
3. **Rebase and Merge**：修复分支使用 Rebase and Merge

---

## 五、CI/CD 配置

### 5.1 GitHub Actions 配置

创建 `.github/workflows/ci.yml`：

```yaml
name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  # 后端测试
  backend-test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: coolsim_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      
      redis:
        image: redis:7
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt
          pip install -r backend/requirements-dev.txt
      
      - name: Run linting
        run: |
          cd backend
          black --check .
          isort --check-only .
          flake8 .
          mypy .
      
      - name: Run tests
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/coolsim_test
          REDIS_URL: redis://localhost:6379/0
          SECRET_KEY: test-secret-key
        run: |
          cd backend
          pytest --cov=apps --cov-report=xml --cov-report=html
      
      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          file: backend/coverage.xml
          flags: backend

  # 前端测试
  frontend-test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: Install dependencies
        run: |
          cd frontend
          npm ci
      
      - name: Run linting
        run: |
          cd frontend
          npm run lint
      
      - name: Run type check
        run: |
          cd frontend
          npm run type-check
      
      - name: Run tests
        run: |
          cd frontend
          npm test -- --coverage
      
      - name: Build
        run: |
          cd frontend
          npm run build

  # 集成测试
  integration-test:
    runs-on: ubuntu-latest
    needs: [ backend-test, frontend-test ]
    
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: coolsim_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      
      redis:
        image: redis:7
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Install backend dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      
      - name: Install frontend dependencies
        run: |
          cd frontend
          npm ci
      
      - name: Run integration tests
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/coolsim_test
          REDIS_URL: redis://localhost:6379/0
          SECRET_KEY: test-secret-key
        run: |
          cd backend
          pytest tests/integration/ -v

  # 代码质量检查
  code-quality:
    runs-on: ubuntu-latest
    needs: [ backend-test, frontend-test ]
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Run SonarCloud analysis
        uses: sonarcloud/gh-action_sonarcloud@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  # 安全扫描
  security-scan:
    runs-on: ubuntu-latest
    needs: [ backend-test, frontend-test ]
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Run Snyk security scan
        uses: snyk/actions/python@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

  # 部署到 staging
  deploy-staging:
    runs-on: ubuntu-latest
    needs: [ integration-test, code-quality, security-scan ]
    if: github.ref == 'refs/heads/develop'
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to staging
        run: |
          echo "Deploying to staging..."
          # 添加部署脚本

  # 部署到 production
  deploy-production:
    runs-on: ubuntu-latest
    needs: [ integration-test, code-quality, security-scan ]
    if: github.ref == 'refs/heads/main'
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to production
        run: |
          echo "Deploying to production..."
          # 添加部署脚本
```

### 5.2 CI 检查流程

#### PR 提交后自动执行

1. **代码风格检查**
   - Python: Black, isort, Flake8, MyPy
   - TypeScript: ESLint, Prettier

2. **单元测试**
   - Python: pytest
   - TypeScript: Jest

3. **集成测试**
   - API 接口测试
   - 数据库集成测试

4. **代码质量检查**
   - SonarCloud 代码质量分析
   - 测试覆盖率检查

5. **安全扫描**
   - Snyk 依赖漏洞扫描
   - 代码安全分析

6. **构建验证**
   - 前端构建
   - 后端打包

#### 检查结果

| 检查项 | 通过条件 | 失败处理 |
|--------|----------|----------|
| 代码风格 | 无警告 | 阻止合并 |
| 单元测试 | 全部通过 | 阻止合并 |
| 集成测试 | 全部通过 | 阻止合并 |
| 代码质量 | 质量门禁通过 | 阻止合并 |
| 安全扫描 | 无高危漏洞 | 阻止合并 |
| 构建验证 | 构建成功 | 阻止合并 |
| 测试覆盖率 | ≥80% | 警告 |

---

## 六、代码审查流程

### 6.1 审查者规则

| 角色 | 职责 | 权限 |
|------|------|------|
| 作者 | 创建 PR、修改代码、回应评论 | 可以修改代码 |
| 审查者 | 审查代码、提出建议、批准/拒绝 | 可以批准/拒绝 |
| 维护者 | 最终审批、合并 PR | 可以合并 |

### 6.2 审查流程

```
1. 作者创建 PR
   ↓
2. CI 自动检查
   ↓
3. 审查者审查代码
   ↓
4. 作者回应评论
   ↓
5. 审查者批准/拒绝
   ↓
6. 维护者最终审批
   ↓
7. 合并 PR
```

### 6.3 审查检查清单

#### 代码质量

- [ ] 代码符合规范
- [ ] 无重复代码
- [ ] 无魔法数字/字符串
- [ ] 适当的注释
- [ ] 命名清晰

#### 功能正确性

- [ ] 功能符合需求
- [ ] 边界条件处理
- [ ] 错误处理完整
- [ ] 性能符合要求

#### 安全性

- [ ] 无 SQL 注入风险
- [ ] 无 XSS 风险
- [ ] 敏感信息已加密
- [ ] 权限验证完整

#### 测试覆盖

- [ ] 新增测试用例
- [ ] 测试覆盖率达标
- [ ] 测试用例质量高

#### 文档更新

- [ ] API 文档已更新
- [ ] 代码注释已添加
- [ ] README 已更新（如需要）

---

## 七、发布流程

### 7.1 版本号规范

```
v主版本号.次版本号.修订号

示例：
v0.5.1
v0.5.2
v0.6.0
v1.0.0
```

### 7.2 发布流程

```
1. 从 develop 创建 release 分支
   git checkout -b release/v0.5.1 develop
   ↓
2. 在 release 分支上进行最后的修复和测试
   ↓
3. 更新版本号和 CHANGELOG
   ↓
4. 合并到 main 并打 tag
   git checkout main
   git merge release/v0.5.1
   git tag -a v0.5.1 -m "Release v0.5.1"
   ↓
5. 合并回 develop
   git checkout develop
   git merge release/v0.5.1
   ↓
6. 删除 release 分支
   git branch -d release/v0.5.1
   ↓
7. 推送到远程
   git push origin main --tags
   git push origin develop
```

### 7.3 CHANGELOG 格式

```markdown
# 变更日志

## [v0.5.1] - 2026-07-10

### 新增
- feat(auth): 添加用户认证功能
- feat(project): 添加项目管理功能

### 变更
- refactor(calculation): 重构计算引擎

### 修复
- fix(calculation): 修复冷量计算精度问题

### 安全
- security(auth): 加强密码加密算法

## [v0.5.0] - 2026-07-01

### 新增
- 初始化项目结构
- 添加基础框架
```

---

## 八、团队协作规范

### 8.1 每日站会

- **时间**：每天 9:30
- **时长**：15 分钟
- **内容**：昨天完成、今天计划、遇到问题

### 8.2 代码评审

- **时间**：PR 提交后 24 小时内
- **方式**：GitHub PR 评论
- **标准**：代码规范、功能正确、安全可靠

### 8.3 文档更新

- **时机**：每次 PR 提交时
- **内容**：API 文档、代码注释、README
- **标准**：清晰、完整、准确

### 8.4 问题跟踪

- **工具**：GitHub Issues
- **格式**：使用 Issue 模板
- **优先级**：P0（紧急）、P1（重要）、P2（一般）、P3（低）

---

## 九、工具配置

### 9.1 Git 配置

```bash
# 设置用户信息
git config user.name "Your Name"
git config user.email "your.email@example.com"

# 设置默认分支
git config init.defaultBranch main

# 设置换行符
git config core.autocrlf true  # Windows
git config core.autocrlf input # Mac/Linux
```

### 9.2 IDE 配置

#### VS Code

安装扩展：
- Python
- Pylance
- Black Formatter
- isort
- Flake8
- MyPy
- TypeScript
- ESLint
- Prettier

#### PyCharm

配置：
- Python 解释器
- 代码风格（Black）
- 导入排序（isort）
- 类型检查（MyPy）

### 9.3 Git Hooks

使用 pre-commit 配置：

```yaml
# .pre-commit-config.yaml
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.4.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-added-large-files

  - repo: https://github.com/psf/black
    rev: 23.3.0
    hooks:
      - id: black

  - repo: https://github.com/pycqa/isort
    rev: 5.12.0
    hooks:
      - id: isort

  - repo: https://github.com/pycqa/flake8
    rev: 6.0.0
    hooks:
      - id: flake8

  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v3.0.0
    hooks:
      - id: prettier
        types_or: [css, javascript, json, markdown, yaml]
```

---

## 十、总结

### 10.1 协作流程

1. **分支策略**：Git Flow 工作流
2. **提交规范**：Conventional Commits
3. **PR 规则**：标准化模板和流程
4. **CI/CD**：自动化检查和部署
5. **代码审查**：多人审查，确保质量

### 10.2 质量保障

1. **代码风格**：统一规范，自动检查
2. **测试覆盖**：单元测试、集成测试、E2E 测试
3. **代码质量**：SonarCloud 分析，质量门禁
4. **安全扫描**：依赖漏洞扫描，代码安全分析
5. **性能监控**：性能测试，资源监控

### 10.3 团队协作

1. **沟通工具**：GitHub Issues, Pull Requests
2. **文档管理**：统一文档体系，及时更新
3. **版本控制**：Git 版本管理，CHANGELOG
4. **发布流程**：标准化发布流程，版本号规范

---

**配置完成时间**：2026-07-10
**配置负责人**：Claude Code
**维护者**：项目团队
