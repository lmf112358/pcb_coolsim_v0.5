# PCB-CoolSim 版本控制与协作指南

> 版本：1.0
> 日期：2026-07-10
> 状态：已完成

---

## 快速开始

### 1. 克隆仓库

```bash
git clone https://github.com/your-org/pcb_coolsim_v0.5.git
cd pcb_coolsim_v0.5
```

### 2. 安装依赖

```bash
# 后端
cd backend
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate  # Windows
pip install -r requirements.txt
pip install -r requirements-dev.txt

# 前端
cd ../frontend
npm install
```

### 3. 配置 pre-commit

```bash
# 安装 pre-commit
pip install pre-commit

# 安装 hooks
pre-commit install

# 运行所有 hooks
pre-commit run --all-files
```

### 4. 创建功能分支

```bash
# 从 develop 分支创建功能分支
git checkout develop
git pull origin develop
git checkout -b feature/F1-001-user-authentication
```

### 5. 开发和提交

```bash
# 开发代码...

# 运行测试
cd backend
pytest
cd ../frontend
npm test

# 提交代码
git add .
git commit -m "feat(auth): 添加用户认证功能

- 实现用户登录接口
- 添加 Token 刷新机制
- 实现权限验证中间件

PRD 引用：F1-001
Spec 引用：模块-01-账户与项目管理.md Section 2"
```

### 6. 推送和创建 PR

```bash
# 推送到远程
git push origin feature/F1-001-user-authentication

# 在 GitHub 上创建 Pull Request
# 使用 PR 模板填写变更说明
```

---

## 分支策略

### 分支类型

| 分支类型 | 命名规范 | 用途 | 生命周期 |
|----------|----------|------|----------|
| 主分支 | `main` | 生产环境代码 | 永久 |
| 开发分支 | `develop` | 开发环境代码 | 永久 |
| 功能分支 | `feature/*` | 开发新功能 | 短期 |
| 修复分支 | `bugfix/*` | 修复 bug | 短期 |
| 发布分支 | `release/*` | 准备发布版本 | 短期 |
| 热修复分支 | `hotfix/*` | 紧急修复生产问题 | 短期 |

### 分支工作流程

```
main (生产)
  ↑
  │ merge (release)
  │
develop (开发)
  ↑
  │ merge (feature/bugfix)
  │
feature/* / bugfix/*
```

### 命名示例

```bash
# 功能分支
feature/F1-001-user-authentication
feature/F2-001-data-collection
feature/F4-001-static-calculation

# 修复分支
bugfix/F1-001-login-error
bugfix/F4-001-calculation-precision

# 发布分支
release/v0.5.1
release/v0.5.2

# 热修复分支
hotfix/v0.5.1-security-fix
hotfix/v0.5.1-critical-bug
```

---

## 提交规范

### 提交信息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 类型说明

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat(auth): 添加用户登录功能` |
| `fix` | 修复 bug | `fix(calculation): 修复冷量计算精度问题` |
| `docs` | 文档更新 | `docs(API): 更新接口文档` |
| `style` | 代码格式 | `style(backend): 格式化代码` |
| `refactor` | 重构 | `refactor(calculation): 重构计算引擎` |
| `perf` | 性能优化 | `perf(database): 优化查询性能` |
| `test` | 测试相关 | `test(calculation): 添加单元测试` |
| `chore` | 其他 | `chore(deps): 更新依赖版本` |

### 范围说明

| 范围 | 说明 |
|------|------|
| `auth` | 认证授权 |
| `project` | 项目管理 |
| `room` | 房间管理 |
| `calculation` | 计算引擎 |
| `simulation` | 动态仿真 |
| `export` | 报告导出 |
| `frontend` | 前端 |
| `backend` | 后端 |
| `database` | 数据库 |
| `docs` | 文档 |

### 提交示例

```bash
# 新功能
git commit -m "feat(auth): 添加 JWT 认证功能

- 实现用户登录接口
- 添加 Token 刷新机制
- 实现权限验证中间件

PRD 引用：F1-001
Spec 引用：模块-01-账户与项目管理.md Section 2"

# 修复 bug
git commit -m "fix(calculation): 修复静态冷量计算精度问题

- 修正焓值计算公式
- 优化浮点数精度处理
- 添加精度验证测试

PRD 引用：F4-001
测试用例：TC-CALC-001"

# 文档更新
git commit -m "docs(API): 更新认证接口文档

- 添加请求/响应格式说明
- 补充错误码定义
- 更新使用示例

PRD 引用：F1-001"
```

---

## Pull Request 流程

### PR 创建

1. **从 develop 分支创建功能分支**
2. **开发和测试代码**
3. **提交代码（遵循提交规范）**
4. **推送到远程仓库**
5. **在 GitHub 上创建 Pull Request**

### PR 模板

使用 PR 模板填写：
- 变更说明
- 变更类型
- 关联需求
- 测试情况
- 代码审查清单
- 依赖变更
- 部署说明
- 回滚方案

### PR 规则

1. **一个 PR 一个功能/修复**
2. **必须关联 PRD 和 Spec**
3. **必须包含测试用例**
4. **必须更新相关文档**
5. **必须通过 CI 检查**

### 代码审查

1. **至少两人审查**
2. **所有检查通过**
3. **无冲突**
4. **代码符合规范**
5. **测试覆盖达标**

### 合并策略

| 分支类型 | 合并策略 | 说明 |
|----------|----------|------|
| 功能分支 | Squash and Merge | 压缩为一个提交 |
| 修复分支 | Rebase and Merge | 变基合并 |
| 发布分支 | Merge Commit | 保留分支历史 |
| 热修复分支 | Merge Commit | 保留分支历史 |

---

## CI/CD 流程

### 自动检查

PR 提交后自动执行：

1. **代码风格检查**
   - Python: Black, isort, Flake8, MyPy
   - TypeScript: ESLint, Prettier

2. **单元测试**
   - Python: pytest
   - TypeScript: Jest

3. **集成测试**
   - API 接口测试
   - 数据库集成测试

4. **代码质量检查**
   - SonarCloud 分析
   - 测试覆盖率检查

5. **安全扫描**
   - Snyk 依赖漏洞扫描
   - 代码安全分析

6. **构建验证**
   - 前端构建
   - 后端打包

### 检查结果

| 检查项 | 通过条件 | 失败处理 |
|--------|----------|----------|
| 代码风格 | 无警告 | 阻止合并 |
| 单元测试 | 全部通过 | 阻止合并 |
| 集成测试 | 全部通过 | 阻止合并 |
| 代码质量 | 质量门禁通过 | 阻止合并 |
| 安全扫描 | 无高危漏洞 | 阻止合并 |
| 构建验证 | 构建成功 | 阻止合并 |
| 测试覆盖率 | ≥80% | 警告 |

### 部署流程

```
develop 分支
  ↓ (CI 检查通过)
部署到 staging 环境
  ↓ (冒烟测试通过)
main 分支
  ↓ (CI 检查通过)
部署到 production 环境
  ↓ (生产测试通过)
创建 Release
```

---

## 代码审查指南

### 审查者规则

| 角色 | 职责 | 权限 |
|------|------|------|
| 作者 | 创建 PR、修改代码、回应评论 | 可以修改代码 |
| 审查者 | 审查代码、提出建议、批准/拒绝 | 可以批准/拒绝 |
| 维护者 | 最终审批、合并 PR | 可以合并 |

### 审查检查清单

#### 代码质量

- [ ] 代码符合规范
- [ ] 无重复代码
- [ ] 无魔法数字/字符串
- [ ] 适当的注释
- [ ] 命名清晰

#### 功能正确性

- [ ] 功能符合需求
- [ ] 边界条件处理
- [ ] 错误处理完整
- [ ] 性能符合要求

#### 安全性

- [ ] 无 SQL 注入风险
- [ ] 无 XSS 风险
- [ ] 敏感信息已加密
- [ ] 权限验证完整

#### 测试覆盖

- [ ] 新增测试用例
- [ ] 测试覆盖率达标
- [ ] 测试用例质量高

#### 文档更新

- [ ] API 文档已更新
- [ ] 代码注释已添加
- [ ] README 已更新（如需要）

---

## 发布流程

### 版本号规则

```
v主版本号.次版本号.修订号

示例：
v0.5.1
v0.5.2
v0.6.0
v1.0.0
```

### 发布流程

```bash
# 1. 从 develop 创建 release 分支
git checkout develop
git pull origin develop
git checkout -b release/v0.5.1

# 2. 在 release 分支上进行最后的修复和测试

# 3. 更新版本号和 CHANGELOG

# 4. 合并到 main 并打 tag
git checkout main
git merge release/v0.5.1
git tag -a v0.5.1 -m "Release v0.5.1"

# 5. 合并回 develop
git checkout develop
git merge release/v0.5.1

# 6. 删除 release 分支
git branch -d release/v0.5.1

# 7. 推送到远程
git push origin main --tags
git push origin develop
```

### CHANGELOG 更新

每次发布都应更新 CHANGELOG.md：
- 记录所有变更
- 按照变更类型分类
- 关联相关的 Issue 和 PR
- 使用中文描述

---

## 团队协作

### 每日站会

- **时间**：每天 9:30
- **时长**：15 分钟
- **内容**：昨天完成、今天计划、遇到问题

### 代码评审

- **时间**：PR 提交后 24 小时内
- **方式**：GitHub PR 评论
- **标准**：代码规范、功能正确、安全可靠

### 文档更新

- **时机**：每次 PR 提交时
- **内容**：API 文档、代码注释、README
- **标准**：清晰、完整、准确

### 问题跟踪

- **工具**：GitHub Issues
- **格式**：使用 Issue 模板
- **优先级**：P0（紧急）、P1（重要）、P2（一般）、P3（低）

---

## 工具配置

### Git 配置

```bash
# 设置用户信息
git config user.name "Your Name"
git config user.email "your.email@example.com"

# 设置默认分支
git config init.defaultBranch main

# 设置换行符
git config core.autocrlf true  # Windows
git config core.autocrlf input # Mac/Linux
```

### IDE 配置

#### VS Code

安装扩展：
- Python
- Pylance
- Black Formatter
- isort
- Flake8
- MyPy
- TypeScript
- ESLint
- Prettier

#### PyCharm

配置：
- Python 解释器
- 代码风格（Black）
- 导入排序（isort）
- 类型检查（MyPy）

### pre-commit 配置

```bash
# 安装 pre-commit
pip install pre-commit

# 安装 hooks
pre-commit install

# 运行所有 hooks
pre-commit run --all-files

# 更新 hooks
pre-commit autoupdate
```

---

## 常见问题

### Q: 如何创建功能分支？

```bash
git checkout develop
git pull origin develop
git checkout -b feature/F1-001-user-authentication
```

### Q: 如何提交代码？

```bash
git add .
git commit -m "feat(auth): 添加用户认证功能

- 实现用户登录接口
- 添加 Token 刷新机制

PRD 引用：F1-001"
```

### Q: 如何解决合并冲突？

```bash
# 拉取最新代码
git checkout develop
git pull origin develop

# 切换到功能分支
git checkout feature/F1-001-user-authentication

# 合并 develop
git merge develop

# 解决冲突后提交
git add .
git commit -m "merge: 合并 develop 分支"
```

### Q: 如何撤销提交？

```bash
# 撤销最后一次提交（保留更改）
git reset --soft HEAD~1

# 撤销最后一次提交（丢弃更改）
git reset --hard HEAD~1
```

### Q: 如何查看提交历史？

```bash
# 查看简洁历史
git log --oneline

# 查看图形化历史
git log --graph --oneline --all

# 查看特定文件的历史
git log --follow -- <file>
```

---

## 联系方式

- **即时通讯**：#coolsim-dev
- **邮箱**：dev@company.com
- **每日站会**：每天 9:30

---

**版本控制与协作指南 - PCB-CoolSim v0.5**
*最后更新：2026-07-10*
