# 2026-07-09 工作日志

## PRD v0.5 需求审核（grill-with-docs）
- 审核对象：`PCB工厂冷源仿真平台_PRD_v0.5_终极合并版.md`（2106 行，PCB-CoolSim 冷源仿真平台）
- 视角：用户交互 → 计算输入 → 计算逻辑 → 计算输出
- 产出文件：
  - `PRD审核报告_v0.5.md`（主报告：🔴4处矛盾/🟠多处/🟡建议，分六章）
  - `CONTEXT.md`（领域术语表，统一"功能区域"等冲突词）
  - `docs/adr/0001~0004.md`（4 项待裁决决策）
- 关键发现（须用户裁决）：
  1. F5-015~F5-034 需求 ID 缺失但被 SAC-15~18 引用（可追溯性断裂）
  2. 静态/动态 新风负值处理不一致（F4-014 vs F6-006）
  3. 两套比例系数表矛盾（§11 vs §10.7，>35℃ 1.1 vs 1.2）
  4. V3 容量占比≤130% 与 N+1 冗余冲突（2+1 必触发 V3）
  5. P&ID/水蓄冷 做不做前后抵触（§9.1 不做 vs §21.1/SAC 做）
  6. 缺异步/调度基础设施（NF 部署栈无 Celery/APScheduler）
  7. 模块编号顺序错乱（模块十在模块六前）、章节子号复用
- 当前状态：已交付报告+术语表+4 ADR，正等待用户对 4 个关键决策（ADR-0001~0004）的裁决

## 用户裁决结果（grill 完成）
1. ADR-0001：末端负荷 **v0.5 即支持房间级冷冻水档分配**（推翻全归中温简化）→ 扩大范围，Room 加 terminal_water_temp_config_id + 表单/Excel/汇总/验收同步改
2. ADR-0004：V3 改对"运行容量（扣1台备用）"校验
3. ADR-0005（新增）：P&ID 生成本期做；水蓄冷仅容量校验、不做 ROI
4. ADR-0002：异步/调度用 Celery + Redis
5. ADR-0003（比例系数表统一）**仍待裁决**
- 产出新增：`已裁决决策汇总.md`、`docs/adr/0005-pid-thermal-storage-scope.md`，并更新 0001/0002/0004/CONTEXT.md 为 accepted
- 决策1 扩大了 v0.5 范围（约 +0.5~1 周），已在 ADR-0001 与汇总中标红

## 输出修订版 PRD（v0.5.1）
- 基于 5 项 ADR 决策 + 全部审核发现，重写整份 PRD 为 `PCB工厂冷源仿真平台_PRD_v0.5.1_修订版.md`（约 24 章）
- 关键修订：模块一→十重排、章节号连续化、补齐 F5-015~026、补 SAC-04/G5/A4/C4、ADR-0001~0005 全部落入正文、附录I 修订对照表
- 新增字段：Room.terminal_water_temp_config_id、temp_precision；DefaultConfig 含压差渗透系数表/空气密度；新增 /api/auth/refresh、P&ID SVG 接口、Celery+Redis（NF-032）
- 排期后端/前端各 +0.5 周
- ADR-0003 比例系数表已在本版以"统一表（>35℃/100%=1.2，待暖通校准）"形式落入正文，状态 accepted（系数值待校准）
