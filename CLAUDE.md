# CLAUDE.md

本文件为 Claude Code（claude.ai/code）在本仓库中工作时提供指导。

## 项目概述

PCB-CoolSim 是一个基于 Web 的 PCB 制造工厂冷量计算与仿真平台。它用自动化、可视化、可协作的工具替代传统手工 Excel 计算。

**核心目标：**
- 静态冷量计算：设计日冷负荷计算，偏差 < 0.1%
- 动态仿真：基于气象数据的 8760 小时全年仿真，偏差 < 1%
- 2D 可视化：负荷分布热力图可视化
- 报告生成：支持 PDF、Excel、CSV 导出
- 负荷预测：未来 7×24 小时预测，天气 API 每小时自动更新

**目标用户：** 暖通工程师、项目经理、系统管理员

**文档体系状态（2026-07-14 架构审核后）：** PRD/系统级/模块级/接口级/实现级/UI 全中文且自洽，三大阻塞性根因（核心计算逻辑/数据库表设计/接口级端点覆盖）已修复，可进入代码开发。

---

## 文档语言规范

**重要：所有文档必须使用中文撰写**

- 代码注释使用中文
- README、PRD、设计文档等所有文档使用中文
- API 文档和说明使用中文
- 测试文档和用户故事使用中文
- Git 提交信息和 PR 描述使用中文
- 错误信息和日志使用中文（面向用户的内容）
- 变量名、函数名、类名使用英文（代码规范要求）
- 技术术语可使用英文（如 React, Django, PostgreSQL 等）

**原则：** 这是一个面向中国用户的项目，所有文档和界面文字都应该使用中文，以确保团队成员和用户能够顺畅地理解项目。

---

## 技术栈

### 前端
- **React 18** + TypeScript + Vite 5
- **Ant Design 5** 提供 UI 组件库
- **React Query 5** 管理服务端状态
- **React Context** 管理 UI 状态
- **Leaflet** 提供地理地图
- **Konva + react-konva** 提供 2D 画布编辑器与热力图；**pdfjs-dist** 渲染 PDF 底图；2D 编辑器内部交互态用 **Zustand** 局部 store 管理
- **ECharts** 提供图表与可视化

### 后端
- **Django 5** + Django REST Framework (DRF)
- **Celery 5** 处理异步任务
- **PostgreSQL 16** 搭配 TimescaleDB 扩展
- **Redis 7** 用于缓存和 Celery broker
- **MinIO/S3** 用于文件存储
- **Nginx** 用于反向代理和负载均衡

---

## SSD 文档工程：从 PRD 到代码的规范路径

### 核心理念

**SSD（Software Specification Document）方法论** 是连接产品需求与代码实现的桥梁。核心原则：

- **PRD 是唯一需求源**：所有功能必须追溯到 PRD 中的需求条目（F1-001, F2-001, 等）
- **Spec 是开发契约**：开发人员只能按照 Spec 编写代码，不能"发挥"
- **四层 Spec 结构**：系统级 → 模块级 → 接口级 → 实现级
- **双向可追溯**：PRD → Spec → Code → Test，任何变更必须全链路同步

### Spec 层次结构

```
PRD（产品需求文档）── 唯一需求源
  ↓ 需求条目映射
00-PRD/
  ├── README.md（PRD 索引）
  └── PRD_v0.5.1.md（产品需求文档）
  ↓ 系统分解
01-系统级-Spec/（System Specification）
  ├── 系统架构设计.md
  ├── 数据库设计.md
  └── API 总览.md
  ↓ 模块分解
02-模块级-Spec/（Module Specification）
  ├── 模块-01-账户与项目管理.md
  ├── 模块-02-数据采集.md
  ├── 模块-03-2D可视化标注.md
  ├── 模块-04-静态冷量计算.md
  ├── 模块-05-负荷分析.md
  ├── 模块-06-动态仿真负荷计算.md
  ├── 模块-07-8760可视化与基础验证.md
  ├── 模块-08-报告与数据导出.md
  ├── 模块-09-系统设置与全局交互.md
  └── 模块-10-负荷预测.md
  ↓ 接口细化
03-接口级-Spec/（Interface Specification，扁平 .md 文件）
  ├── 认证接口.md / 项目接口.md / 房间接口.md / 计算接口.md
  ├── 仿真接口.md / 导出接口.md / 气象接口.md / 平面图接口.md
  ├── 负荷预测接口.md / 对话采集接口.md / 数据管理接口.md
  ├── 表结构详细设计.md / 索引设计.md / 分区策略.md
  └── 数据流设计.md / 状态管理.md / 组件规范.md
  ↓ 实现指导
04-实现级-Spec/（Implementation Specification，扁平 .md 文件）
  ├── 算法伪代码.md / 服务层设计.md / 服务层模板.md / 数据访问层设计.md
  ├── 模型层模板.md / 控制器层模板.md / 异步任务设计.md
  ├── 状态管理实现.md / 前端组件模板.md / 组件实现规范.md / 页面实现规范.md
  ├── 测试用例设计.md / 追溯性矩阵.md
  ├── 8760h 数据处理实现.md / 动态仿真计算实现.md
  └── 计算逻辑/静态冷量计算实现.md
```

### 可追溯性矩阵

每个需求条目必须建立完整的追溯链：

| PRD 需求 | Spec 文档 | 测试用例 | 状态 |
|----------|----------|----------|------|
| F2-001 | module-01-account.md Section 2 | TC-AUTH-001 | ✅ |
| F2-002 | module-02-data-collection.md Section 3 | TC-DATA-001 | ✅ |
| F4-001 | module-04-static-calculation.md Section 4 | TC-CALC-001 | ✅ |

**追溯规则：**
- 每个 PRD 需求必须有对应的 Spec 文档
- 每个 Spec 必须有对应的测试用例
- 任何变更必须更新整个追溯链
- 缺失追溯的需求不能进入开发

---

## 开发工作流

### 质量门禁（5 阶段）

1. **需求门禁**：PRD 完成、评审、批准
2. **设计门禁**：架构、API、UI 设计完成，无矛盾
3. **实现门禁**：代码完成，测试通过
4. **测试门禁**：所有测试通过，缺陷已解决
5. **发布门禁**：所有门禁通过，文档完整

### 分支策略

```bash
# 创建功能分支
git checkout -b feature/my-feature

# 开发并提交
git commit -m "feat(scope): description"

# 向 main 创建 PR
# 评审通过后合并
```

---

## 关键文档

### 核心文档（L0-L1）

| 文档 | 位置 | 何时参考 |
|----------|----------|-------------------|
| PRD v0.5.1 | `00-PRD/PRD_v0.5.1.md` | 了解功能需求 |
| 系统架构设计 | `01-系统级-Spec/系统架构设计.md` | 了解四层架构 |
| 数据库设计 | `01-系统级-Spec/数据库设计.md` | 数据库表结构与关系（27 张表） |
| API 总览 | `01-系统级-Spec/API总览.md` | API 端点详情（60+ 端点） |

### 模块级 Spec（L2）

| 文档 | 位置 | 何时参考 |
|----------|----------|-------------------|
| 模块 01：账户与项目管理 | `02-模块级-Spec/模块-01-账户与项目管理.md` | 认证与 RBAC 实现 |
| 模块 02：数据采集 | `02-模块级-Spec/模块-02-数据采集.md` | 层级管理 + 对话式采集（§6.10） |
| 模块 04：静态冷量计算 | `02-模块级-Spec/模块-04-静态冷量计算.md` | 6 步计算流水线 |
| 模块 06：动态仿真负荷计算 | `02-模块级-Spec/模块-06-动态仿真负荷计算.md` | 气象驱动仿真 |

### 接口级 Spec（L3）

| 文档 | 位置 | 何时参考 |
|----------|----------|-------------------|
| 表结构详细设计 | `03-接口级-Spec/表结构详细设计.md` | 数据库实现（27 表 PascalCase） |
| 各业务接口 | `03-接口级-Spec/{认证,项目,房间,计算,仿真,导出,气象,平面图,负荷预测,对话采集,数据管理}接口.md` | API 端点实现 |
| 索引/分区/数据流/状态/组件 | `03-接口级-Spec/{索引设计,分区策略,数据流设计,状态管理,组件规范}.md` | 性能与前端实现 |

### 实现级 Spec（L4）

| 文档 | 位置 | 何时参考 |
|----------|----------|-------------------|
| 算法伪代码 | `04-实现级-Spec/算法伪代码.md` | 静态/动态/预测核心算法（含压差8点查表） |
| 静态冷量计算实现 | `04-实现级-Spec/计算逻辑/静态冷量计算实现.md` | 6 步流水线公式 |
| 动态仿真/8760h 实现 | `04-实现级-Spec/{动态仿真计算实现,8760h 数据处理实现}.md` | NumPy 向量化 + TimescaleDB |
| 服务层/模型层/控制器模板 | `04-实现级-Spec/{服务层,模型层,控制器层}*.md` | 后端代码模板 |

---

## 重要说明

1. **计算精度至关重要** — 所有计算必须在与参考数据的容差范围内匹配（静态 < 0.1%、动态 < 1%）
2. **需要 Python 3.11+** — 使用最新特性和类型注解
3. **需要 Node.js 18+** — 用于前端构建工具
4. **需要 PostgreSQL 16+ + TimescaleDB** — 动态仿真逐时数据用超表
5. **需要 Redis 7+** — 用于 Celery broker 和缓存
6. **遵循质量门禁** — 发布前必须通过全部 5 个阶段
7. **追溯性** — 代码需关联 PRD 需求 ID（如 PRD: F2-001）
8. **命名基线（v0.5.1）** — 表名 PascalCase（ORM 模型名），字段名 snake_case，API 路径 `/api/v1/`；比例系数表统一 `RatioCoefficient`
9. **ADR 状态** — 0001/0002/0003/0006/0007 Accepted；0004/0005 Superseded（冷站已移除，勿据此实现）

---

## 文档一致性规则（PRD 为唯一事实源）

> 本规则用于指导全量文档的维护与重构，遵循**奥卡姆剃刀原则**：不引入不必要的实体/表/章节；移除冗余；复用既有结构；PRD 变更后依赖文档必须同步。

### 1. 事实源与依赖方向
- **PRD（`00-PRD/PRD_v0.5.1.md`）是唯一需求与数据模型事实源**。
- 依赖链：PRD → 01-系统级-Spec → 02-模块级-Spec → 03-接口级-Spec → 04-实现级-Spec → 05-UI-UX-设计 → 06-测试文档 → 07-质量保障 → 08-运维文档 → 09-参考文档（术语表/ADR/用户故事） → 10-管理文档。
- PRD 变更时，**下游所有文档必须同步**，不允许遗留断裂的交叉引用（章节号、表名、`SAC-xx`、附录 H）。

### 2. 当前必须向下游传播的事实增量
- **模块五「冷站配置」已移除**（仅保留负荷分析）：所有 冷站配置 / 水蓄冷 / PID 相关描述须移除或收敛；相关 ADR 中 `ADR-0004`（冷站容量校验）、`ADR-0005`（PID/水蓄冷）均已标记 **Superseded (v0.5.1)**，正文保留作历史，请勿据此实现。
- **模块二新增「对话式采集」（§6.10）**：须在 API 总览、数据库设计、系统架构设计、相关模块 Spec、页面设计总览、测试策略/计划、用户故事、完成报告等处同步新增。
- **SOP 对齐既有数据字段**：对话采集六阶段与 `Project`/`WaterTempConfig`/`Building`/`Floor`/`Room`/`ExtraLoad`/`RoomCalcResult`/`LoadSummary` 严格对应。
- **对话采集落表（ADR-0007）**：新增 `ConversationSession` + `ConversationMessage` 两表支撑断点续采（F2-057）与消息历史回看（F2-051/F2-058），PRD 附录 E 原 22 张表未列，由 ADR-0007 补充。
- **数据库共 27 张表**：PRD 附录 E 基线 22 张 + ADR-0006（User/Role/RatioCoefficient/SimulationRun 等）+ ADR-0007（对话 2 表）工程细化。表名统一 **PascalCase**（如 `RoomCalcResult`/`ForecastScenario`/`DynamicLoadHourly`），字段名 snake_case。
- **Room 合并为单表**（PRD 附录 A）：负荷/风量/温湿度/新风配置/末端冷冻水档全部直存 Room 表 39 字段；已删除 `room_load_parameter`/`room_air_volume_parameter`/`room_air_volume_result` 三子表与 `season_config` 表及所有 `season_id`（v0.5.1 不需季节表，气象数据本身区分季节）。
- **工作台 Tab3 = 负荷分析**：6 Tab 完整（Tab1 基础配置 / Tab2 静态计算 / Tab3 负荷分析 / Tab4 动态仿真 / Tab5 2D展示 / Tab6 负荷预测）。对话采集页为独立全屏三栏 Codex 布局。
- **核心计算基线（PRD §15，不可违背）**：① 末端负荷 5 项（含电热设备，kW 不乘 1000）；② 压差渗透 8 点查表线性插值（§8.3 唯一权威表，绝不用 k=ΔP/5 或 √ΔP）；③ 新风量简单加法（无 MAX/卫生/净化风量，A4 简化）；④ 新风冷负荷 ρ=1.2、室内默认 26°C/55%、定义 A 唯一公式；⑤ 总负荷 < 0 截断为 0；⑥ 末端按 `terminal_water_temp_config_id` 归档（ADR-0001，默认中温）；⑦ 动态方式二查 `RatioCoefficient` 取负荷率=100% 列。
- **2D 画布引擎定为 Konva + react-konva + pdfjs-dist（v0.5.2 原型对齐）**：原 Fabric.js 已替换；平面图 JSON 在 v1 基础上向后兼容扩展可选字段（vertices / z_index / locked / visible / legend_code / style / metadata），version 保持 1。2D 编辑器（Floor2DEditor）内部交互态用 Zustand 局部 store；服务端数据仍走 React Query（见 04-实现级-Spec/状态管理实现.md §3.6）。
- **模块-01 RBAC 细粒度权限码（v0.5.2 原型对齐）**：F1-029 预留接口具体化为 5 模块 12 码（simulation/monitor/report/equipment/system）；双层守卫（路由级 ProtectedRoute + 组件级 PermissionGuard）；access token 内存存储 + refresh token HttpOnly Cookie + 401 并发刷新队列。后端技术栈基线不变（Django+DRF+simplejwt），factory-management 原型（FastAPI）仅作设计参考。

### 3. 奥卡姆剃刀约束
- 不新增未在 PRD 出现的实体/表/章节；确需补充时先在 `09-参考文档/架构决策记录/` 落 ADR（如 ADR-0006 缺失实体补齐、ADR-0007 对话采集表）。
- ADR 复用 `09-参考文档/架构决策记录/`，不另建 `docs/adr/`。
- 删除内容时同步清理所有引用，不留死链。
- 文档语言规范（见上文「文档语言规范」）：全部中文；代码标识符英文。
- 命名一致性：表名 PascalCase，字段名 snake_case，API 路径 `/api/v1/`；比例系数表 `RatioCoefficient`（非 coefficient_matrix）。

### 4. 开发准入（v0.5.1 架构审核结论）
- 文档体系已自洽，可进入 vibe coding。
- 推荐分批解锁：第一批（模块1 认证 / 模块3 2D编辑器 / 前端脚手架）→ 第二批（模块4 静态计算 / 模块2 数据采集 / 模块9 系统设置）→ 第三批（模块6 动态仿真 / 模块10 负荷预测 / 模块7/8）→ 第四批（对话采集页 / 模块5 负荷分析）。
